# PAUL Handoff

**Date:** 2026-04-25d
**Status:** Paused — Phases 10-01/02/03 complete; Phase 10-04 ready to Plan

---

## READ THIS FIRST

You have no prior context. This document is self-contained.

**Project:** 8BitPitch — browser-based mental math trainer, 8-bit retro aesthetic. 8 financial drills. Single HTML file, no server, no frameworks. Global `window.BitPitch` namespace. Folder: `/Users/collinphan/Documents/1 Programming/Claude Projects/8BitPitch/`

---

## Current State

**Version:** v0.4 (Milestone 4 complete, Milestone 5 in progress)
**Milestone:** Milestone 5 — Draft 5: New Features
**Phase:** 10 of 17 (AV Feedback + UI Overhaul)
**Status:** 10-01/02/03 DONE; 10-04 and 10-05 remaining

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Phase 10-01 — Favicon — complete]
  ✓        ✓        ✓     [Phase 10-02 — Sound + Vibration System — complete]
  ✓        ✓        ✓     [Phase 10-03 — Mode Bar Redesign + News Screen — complete]
  ○        ○        ○     [Phase 10-04 — Splash + Log-In Entry Screen — next]
```

---

## What Was Done This Session

### Phase 10-01 — Favicon
- Replaced `<link rel="icon" href="data:,">` with inline SVG favicon in `index.html` line 11
- Yellow "8" (#FFD700) on black rounded square (rx=6), monospace font
- User tested: confirmed working in browser

### Phase 10-02 — Sound + Vibration System
- New file: `utils/sounds.js` — `window.BitPitch.Sound` module
- Correct sound: ascending square-wave arpeggio (C5/E5/G5, 70ms staggered)
- Wrong sound: descending sawtooth buzz (220Hz→80Hz, 300ms)
- Vibration: 80ms on correct; [100, 50, 100] on wrong (where supported)
- SFX toggle button (green = on, dim = muted) added to all 8 exercise flex-rows
- localStorage persistence: key `bp_sound` ('0' = muted)
- `utils/sounds.js` added to script load order in `index.html`
- User tested on browser: confirmed working. **Mobile vibration untested** — deferred to next git push
- New CSS: `.sound-toggle-btn`, `.sound-toggle-btn.muted` in `style.css`

### Phase 10-03 — Mode Bar Redesign + News Screen
- Mode bar redesigned: 4 tabs → 3 tabs
  - `tab-mode-select` (MODE SELECT + sub-label showing DRILL or PRACTICE)
  - `tab-profile` (PROFILE & SETTINGS)
  - `tab-news` (8BITPITCH NEWS)
- Three new screen shells added to `index.html`:
  - `#screen-mode-select` — empty, for Phase 12-01
  - `#screen-profile` — empty, for Phases 11-03 / 12-03
  - `#screen-news` — has header + MENU button + "COMING SOON" placeholder
- `syncModeTabs()` updated: drill/practice/mode-select all highlight `tab-mode-select`
- `AppState.setMode()` updated for new modes; old `scores` route removed
- New handlers: `showModeSelect`, `showProfile`, `showNews`, `showDifficultyExplained`
- `renderHome()` updates `#tab-mode-sublabel` on each call
- **DIFFICULTY EXPLAINED** removed from mode bar — still accessible via `AppState.showDifficultyExplained()`. Will be linked from Profile & Settings in Phase 12-03.
- `.mode-tab-sub` CSS added to `style.css`
- User tested: confirmed working

---

## What's Next

**Immediate:** Plan and apply Phase 10-04 — Splash + Log-In Entry Screen

**10-04 scope (from ROADMAP/PROJECT.md):**
- Splash screen: words "8", "Bit", "Pitch", "Lite" drop in from above sequentially, then fade to log-in entry screen
- Log-in entry screen: two buttons — PLAY AS GUEST (goes to home) and CREATE BETA ACCOUNT (Phase 11 — stub for now)
- This screen sits before the home screen; it's what the user sees on first load

**Execution order for Phase 10 remainder:**
10-04 → 10-05

**Before Phase 11-01 (not blocking 10-04/10-05):**
- [ ] Retrieve Supabase anon key (dashboard → Project Settings → API)
- [ ] Disable email confirmation (Auth → Providers → Email)
- [ ] Vercel deployment live (Supabase Auth requires HTTPS)

**Before Phase 13-03:**
- [ ] Confirm Google Form trigger wording ("After hitting..." — sentence was cut off)

---

## Key Context

| Item | Value |
|------|-------|
| Supabase project ID | cfllnyvnktuxgiebgryd |
| Supabase anon key | Not yet retrieved — needed for Phase 11 |
| Username storage | `u@bitpitch.local` internally; user only sees username |
| PIN format | 6 digits only (client-validated) |
| Level 1 — easy exercises (5Q) | 25 correct answers (ex01–ex05) |
| Level 1 — harder exercises (3Q) | 15 correct answers (ex06–ex08) |
| Google Form URL | https://forms.gle/Q1gpSmKsx6o5AGTT8 |
| Git remote | https://github.com/collin-ai/8BitPitch.git |

---

## Key Files Reference

| File | Role |
|------|------|
| `.paul/STATE.md` | Full project state and plan tables |
| `.paul/ROADMAP.md` | All milestones and sub-plans |
| `.paul/PROJECT.md` | Requirements and tech stack |
| `utils/sounds.js` | Sound + vibration module (new in 10-02) |
| `index.html` | All screens — mode bar updated, 3 new screen shells added |
| `app.js` | AppState; navigation; syncModeTabs |
| `style.css` | All styles |
| `.paul/phases/10-av-ui-overhaul/10-04` | Next plan to write |
| `.paul/phases/11-auth-ads-backend/11-01-PLAN.md` | Beta Auth Workflow |
| `.paul/phases/12-mode-profile/12-01-PLAN.md` | Mode Select Screen |
| `.paul/phases/13-teaser-news/13-01-PLAN.md` | Teaser Screen |

---

## Resume Instructions

1. Read this handoff
2. Read `.paul/STATE.md` for current loop position
3. Run `/paul:plan` for Phase 10-04 (Splash + Log-In Entry Screen)

---

*Handoff created: 2026-04-25d — supersedes HANDOFF-2026-04-25c.md*
