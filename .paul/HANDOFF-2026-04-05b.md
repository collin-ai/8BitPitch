# PAUL Handoff

**Date:** 2026-04-05b (mid-session — Phase 07 planned, apply interrupted by context limit)
**Status:** paused — 6 plans fully designed; one file partially touched

---

## READ THIS FIRST

You have no prior context. This document is self-contained.

**Project:** BitPitch — browser-based mental math trainer, 8-bit retro aesthetic. 8 financial drills. Single HTML file, no server, no frameworks. Global `window.BitPitch` namespace (file:// compatibility).

---

## Current State

```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✗        ✗     [Phase 07 planned — apply not started]
```

**One file already changed this session:**
- `utils/timer.js` — `window.BitPitch.formatElapsed(sec, showDecimals)` added at bottom. **Do NOT add it again.**

**All other files are unchanged from Milestone 3.**

---

## User Preferences (confirmed this session)

- `formatElapsed(sec)` — live in-drill timer: integer seconds below 60s, `m:ss.t` at/above 60s
- `formatElapsed(sec, true)` — feedback boxes and summary stats: 1dp below 60s, `m:ss.t` at/above 60s

---

## APPLY — File-by-File Checklist

Work through each file in order. Each entry shows the exact old → new text.

---

### FILE 1: `utils/scoring.js`

**Change (07-02):** Extract sumMs; return raw floats for avgSec/fastestSec; add totalMs.

**Find:**
```js
    var avgMs    = total > 0 ? Math.round(times.reduce(function (s, t) { return s + t; }, 0) / total) : 0;
    var fastestMs = total > 0 ? Math.min.apply(null, times) : 0;
    return {
      total:      total,
      correct:    correct,
      accuracy:   total > 0 ? Math.round((correct / total) * 100) : 0,
      avgSec:     (avgMs / 1000).toFixed(1),
      fastestSec: (fastestMs / 1000).toFixed(1),
      answers:    answers.slice()
    };
```

**Replace with:**
```js
    var sumMs    = times.reduce(function (s, t) { return s + t; }, 0);
    var avgMs    = total > 0 ? Math.round(sumMs / total) : 0;
    var fastestMs = total > 0 ? Math.min.apply(null, times) : 0;
    return {
      total:      total,
      correct:    correct,
      accuracy:   total > 0 ? Math.round((correct / total) * 100) : 0,
      avgSec:     avgMs / 1000,
      fastestSec: fastestMs / 1000,
      totalMs:    sumMs,
      answers:    answers.slice()
    };
```

---

### FILE 2: `exercises/ex01-percentage-snap.js`

**Change A (07-05):** Remove time-goal div. Delete this line from `showQuestion()` innerHTML:
```js
        (context.mode === 'drill' ? '<div class="time-goal">GOAL: UNDER <span class="goal-num">10 SECONDS</span></div>' : '') +
```

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```

**Replace with:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change C (07-03):** Remove feedback icon.

**Find:**
```js
    var msg = isOk ? '<span class="feedback-icon">✓</span> CORRECT!' : '<span class="feedback-icon">✗</span> WRONG';
```
**Replace with:**
```js
    var msg = isOk ? 'CORRECT!' : 'WRONG';
```

**Change D (07-04):** Feedback time format.

**Find:**
```js
        (context.mode === 'drill' ? '<br>Time: ' + (elapsed/1000).toFixed(1) + 's' : '') +
```
**Replace with:**
```js
        (context.mode === 'drill' ? '<br>Time: ' + window.BitPitch.formatElapsed(elapsed/1000, true) : '') +
```

---

### FILE 3: `exercises/ex02-multiples.js`

**Change A (07-01):** Add Enter-to-submit on both inputs.

**Find:**
```js
    var lowInput  = document.getElementById('low-input');
    var highInput = document.getElementById('high-input');
    lowInput.focus();
    timer.start();
```
**Replace with:**
```js
    var lowInput  = document.getElementById('low-input');
    var highInput = document.getElementById('high-input');
    lowInput.focus();
    timer.start();

    [lowInput, highInput].forEach(function (inp) {
      inp.onkeydown = function (e) {
        if (e.key !== 'Enter') return;
        e.stopPropagation();
        var sb = document.getElementById('submit-btn');
        if (sb && !sb.disabled) sb.click();
      };
    });
```

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change C (07-03):** Remove feedback icon.

**Find:**
```js
    var msg = isOk ? '<span class="feedback-icon">✓</span> GOOD RANGE!' : '<span class="feedback-icon">✗</span> RANGE OFF';
```
**Replace with:**
```js
    var msg = isOk ? 'GOOD RANGE!' : 'RANGE OFF';
```

**Change D (07-04):** Feedback time format. Same find/replace as ex01 Change D.

---

### FILE 4: `exercises/ex03-double-triple-halve.js`

**Change A (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var timerInterval;
    if (context.mode === 'drill') {
      timerInterval = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerInterval); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    var timerInterval;
    if (context.mode === 'drill') {
      timerInterval = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerInterval); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change B (07-03):** Remove feedback icon.

**Find:**
```js
    var feedbackMsg   = isOk ? '<span class="feedback-icon">✓</span> CORRECT!' : '<span class="feedback-icon">✗</span> WRONG';
```
**Replace with:**
```js
    var feedbackMsg   = isOk ? 'CORRECT!' : 'WRONG';
```

**Change C (07-04):** Feedback time format.

**Find:**
```js
        (context.mode === 'drill' ? '&nbsp;&nbsp;(' + (elapsed/1000).toFixed(1) + 's)' : '') +
```
**Replace with:**
```js
        (context.mode === 'drill' ? '&nbsp;&nbsp;(' + window.BitPitch.formatElapsed(elapsed/1000, true) + ')' : '') +
```

---

### FILE 5: `exercises/ex04-triangle-drill.js`

**Change A (07-05):** Remove time-goal div. Delete this line from `showQuestion()` innerHTML:
```js
        (context.mode === 'drill' ? '<div class="time-goal">GOAL: UNDER <span class="goal-num">' + TIME_GOAL_SEC + ' SECONDS</span></div>' : '') +
```
> Keep `var TIME_GOAL_SEC = 15;` — still used for the warning color threshold below.

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed. Note: ex04 uses `TIME_GOAL_SEC` not `10` for warning threshold.

**Find:**
```js
    clearInterval(timerIv);
    if (context.mode === 'drill') {
      timerIv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerIv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > TIME_GOAL_SEC ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    clearInterval(timerIv);
    if (context.mode === 'drill') {
      timerIv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerIv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > TIME_GOAL_SEC ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change C (07-03):** Fix userVal bug in `submit()` AND remove feedback icon.

**Find** (the entire submit function):
```js
    function submit() {
      clearInterval(timerIv);
      var elapsed = timer.stop();
      var userVal = R.parseUserNumber(input.value);
      var isOk    = close(userVal, correctAnswer, 5);

      var sb = document.getElementById('submit-btn');
      if (sb) { sb.disabled = true; sb.classList.add('btn-submitted'); }
      input.classList.add('input-submitted');

      context.onComplete({
        exerciseId: 'ex04',
        question:   type + ' triangle',
        correct:    isOk,
        timeMs:     elapsed
      });

      showFeedback(type, tri, correctAnswer, isOk, elapsed, false, null);
    }
```
**Replace with:**
```js
    function submit() {
      clearInterval(timerIv);
      var elapsed  = timer.stop();
      var rawVal   = R.parseUserNumber(input.value);
      var isOk     = close(rawVal, correctAnswer, 5);

      var sb = document.getElementById('submit-btn');
      if (sb) { sb.disabled = true; sb.classList.add('btn-submitted'); }
      input.classList.add('input-submitted');

      context.onComplete({
        exerciseId: 'ex04',
        question:   type + ' triangle',
        correct:    isOk,
        timeMs:     elapsed
      });

      var displayUserVal = type === 'equity' ? (rawVal + '%') : R.formatMoney(rawVal);
      showFeedback(type, tri, correctAnswer, isOk, elapsed, false, displayUserVal);
    }
```

**Change D (07-03):** Remove feedback icon in `showFeedback`.

**Find:**
```js
    var msg = isOk ? '<span class="feedback-icon">✓</span> CORRECT!' : '<span class="feedback-icon">✗</span> WRONG';
```
**Replace with:**
```js
    var msg = isOk ? 'CORRECT!' : 'WRONG';
```

**Change E (07-04):** Feedback time format. Same find/replace as ex01 Change D.

---

### FILE 6: `exercises/ex05-growth-rate.js`

**Change A (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change B (07-03):** Remove feedback icon.

**Find:**
```js
    var msg = isOk ? '<span class="feedback-icon">✓</span> CORRECT!' : '<span class="feedback-icon">✗</span> WRONG';
```
**Replace with:**
```js
    var msg = isOk ? 'CORRECT!' : 'WRONG';
```

**Change C (07-04):** Feedback time format.

**Find:**
```js
        (context.mode === 'drill' ? '&nbsp;&nbsp;(' + (elapsed/1000).toFixed(1) + 's)' : '') +
```
**Replace with:**
```js
        (context.mode === 'drill' ? '&nbsp;&nbsp;(' + window.BitPitch.formatElapsed(elapsed/1000, true) + ')' : '') +
```

---

### FILE 7: `exercises/ex06-break-even.js`

**Change A (07-01):** Add Enter-to-submit on both inputs.

**Find:**
```js
    var profitInput = document.getElementById('profit-input');
    var unitsInput  = document.getElementById('units-input');
    profitInput.focus();
    timer.start();
```
**Replace with:**
```js
    var profitInput = document.getElementById('profit-input');
    var unitsInput  = document.getElementById('units-input');
    profitInput.focus();
    timer.start();

    [profitInput, unitsInput].forEach(function (inp) {
      inp.onkeydown = function (e) {
        if (e.key !== 'Enter') return;
        e.stopPropagation();
        var sb = document.getElementById('submit-btn');
        if (sb && !sb.disabled) sb.click();
      };
    });
```

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change C (07-03):** Remove feedback icon.

**Find:**
```js
    var msg = isOk ? '<span class="feedback-icon">✓</span> CORRECT!' : '<span class="feedback-icon">✗</span> CHECK BELOW';
```
**Replace with:**
```js
    var msg = isOk ? 'CORRECT!' : 'CHECK BELOW';
```

**Change D (07-04):** Feedback time format. Same find/replace as ex01 Change D.

---

### FILE 8: `exercises/ex07-tam-estimation.js`

**Change A (07-01):** Add Enter-to-submit (both inputs must be filled).

**Find:**
```js
    var custInput  = document.getElementById('cust-input');
    var spendInput = document.getElementById('spend-input');
    custInput.focus();
    timer.start();
```
**Replace with:**
```js
    var custInput  = document.getElementById('cust-input');
    var spendInput = document.getElementById('spend-input');
    custInput.focus();
    timer.start();

    function trySubmit07(e) {
      if (e.key !== 'Enter') return;
      e.stopPropagation();
      var ci = document.getElementById('cust-input');
      var si = document.getElementById('spend-input');
      if (ci && si && ci.value.trim() !== '' && si.value.trim() !== '') {
        var sb = document.getElementById('submit-btn');
        if (sb && !sb.disabled) sb.click();
      }
    }
    custInput.onkeydown  = trySubmit07;
    spendInput.onkeydown = trySubmit07;
```

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    var iv;
    if (context.mode === 'drill') {
      iv = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(iv); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

> Note: ex07 has no feedback time line to update (self-graded; time not shown in feedback).

---

### FILE 9: `exercises/ex08-valuation-challenge.js`

**Change A (07-01):** Add Enter-to-submit (all three inputs must be filled).

**Find:**
```js
    document.getElementById('val-input').focus();
    timer.start();
```
**Replace with:**
```js
    document.getElementById('val-input').focus();
    timer.start();

    function trySubmit08(e) {
      if (e.key !== 'Enter') return;
      e.stopPropagation();
      var vi = document.getElementById('val-input');
      var mi = document.getElementById('mult-input');
      var bi = document.getElementById('be-input');
      if (vi && mi && bi && vi.value.trim() !== '' && mi.value.trim() !== '' && bi.value.trim() !== '') {
        var sb = document.getElementById('submit-all-btn');
        if (sb && !sb.disabled) sb.click();
      }
    }
    document.getElementById('val-input').onkeydown  = trySubmit08;
    document.getElementById('mult-input').onkeydown = trySubmit08;
    document.getElementById('be-input').onkeydown   = trySubmit08;
```

**Change B (07-04):** Timer interval 1000→100ms, use formatElapsed.

**Find:**
```js
    if (context.mode === 'drill') {
      timerInterval = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerInterval); return; }
        var s = Math.floor(timer.getElapsedSeconds());
        tq.textContent = s + 's';
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 1000);
    }
```
**Replace with:**
```js
    if (context.mode === 'drill') {
      timerInterval = setInterval(function () {
        var tq = document.getElementById('drill-timer-q');
        if (!tq) { clearInterval(timerInterval); return; }
        var s = timer.getElapsedSeconds();
        tq.textContent = window.BitPitch.formatElapsed(s);
        tq.className = s > 10 ? 'drill-timer-q warning' : 'drill-timer-q';
      }, 100);
    }
```

**Change C (07-03):** Remove feedback icon.

**Find:**
```js
    var msg = overallOk
      ? '<span class="feedback-icon">✓</span> SHARP INVESTOR!'
      : (gaveUp ? '<span class="feedback-icon">✗</span> GAVE UP' : '<span class="feedback-icon">✗</span> KEEP TRAINING');
```
**Replace with:**
```js
    var msg = overallOk ? 'SHARP INVESTOR!' : (gaveUp ? 'GAVE UP' : 'KEEP TRAINING');
```

**Change D (07-04):** Feedback time format. Same find/replace as ex01 Change D.

**Change E (07-01):** Add Enter-to-next in `showFeedback`. Also add `document.onkeydown = null` to next-btn onclick.

**Find:**
```js
    document.getElementById('next-btn').onclick = function () {
      context.timerEl.textContent = '';
      timer.reset();
      nextRound();
    };
```
**Replace with:**
```js
    document.getElementById('next-btn').onclick = function () {
      document.onkeydown = null;
      context.timerEl.textContent = '';
      timer.reset();
      nextRound();
    };

    document.onkeydown = function (e) {
      if (e.key === 'Enter') { var nb = document.getElementById('next-btn'); if (nb) nb.click(); }
    };
```

---

### FILE 10: `app.js`

**Change A (07-05):** Remove brackets from difficulty text in `startExercise()`.

**Find:**
```js
  document.getElementById('drill-difficulty').textContent = '[ ' + AppState.difficulty + ' ]';
```
**Replace with:**
```js
  document.getElementById('drill-difficulty').textContent = AppState.difficulty;
```

**Change B (07-05):** Remove brackets from difficulty text in `_setDifficulty()`.

**Find:**
```js
  document.getElementById('drill-difficulty').textContent = '[ ' + lvl + ' ]';
```
**Replace with:**
```js
  document.getElementById('drill-difficulty').textContent = lvl;
```

**Change C (07-06):** Remove ready-hint div from `startExercise()`.

**Find:**
```js
      '<div class="ready-hint">USE THE RESTART BUTTON ABOVE TO RESTART AT ANY TIME</div>' +
```
Delete this line entirely (the `+` on the line above will then connect to `'<div class="diff-row"...'`).

**Change D (07-06):** Replace `restartDrill()` with modal version and add `_showRestartModal()`.

**Find:**
```js
AppState.restartDrill = function () {
  if (!AppState.currentExercise) return;
  if (AppState.drillStarted && !confirm('RESTART DRILL?')) return;
  AppState.startExercise(AppState.currentExercise);
};
```
**Replace with:**
```js
AppState.restartDrill = function () {
  if (!AppState.currentExercise) return;
  if (AppState.drillStarted) {
    AppState._showRestartModal();
  } else {
    AppState.startExercise(AppState.currentExercise);
  }
};

AppState._showRestartModal = function () {
  var overlay = document.getElementById('modal-overlay');
  overlay.classList.remove('hidden');
  document.getElementById('modal-confirm-btn').onclick = function () {
    overlay.classList.add('hidden');
    AppState.startExercise(AppState.currentExercise);
  };
  document.getElementById('modal-cancel-btn').onclick = function () {
    overlay.classList.add('hidden');
  };
};
```

**Change E (07-02):** Replace entire `showSummary()` function.

**Find:**
```js
AppState.showSummary = function () {
  var summary = AppState.session.getSummary();
  var statsEl = document.getElementById('summary-stats');

  var accuracyColor = summary.accuracy >= 80 ? 'var(--green)' :
                      summary.accuracy >= 50 ? 'var(--yellow)' : 'var(--red)';

  statsEl.innerHTML =
    '<div class="stat-row">' +
      '<span class="stat-label">QUESTIONS</span>' +
      '<span class="stat-value accent">' + summary.total + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">CORRECT</span>' +
      '<span class="stat-value">' + summary.correct + ' / ' + summary.total + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">ACCURACY</span>' +
      '<span class="stat-value" style="color:' + accuracyColor + '">' + summary.accuracy + '%</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">AVG TIME</span>' +
      '<span class="stat-value">' + summary.avgSec + 's</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">FASTEST</span>' +
      '<span class="stat-value text-yellow">' + summary.fastestSec + 's</span>' +
    '</div>';

  // Wire play again button to current exercise
  var meta = EXERCISES.find(function (e) { return e.id === AppState.lastExercise; });
  if (meta) {
    document.getElementById('play-again-btn').textContent = 'PLAY AGAIN';
  }

  showScreen('summary');
};
```
**Replace with:**
```js
AppState.showSummary = function () {
  var summary = AppState.session.getSummary();
  var statsEl = document.getElementById('summary-stats');
  var fe      = window.BitPitch.formatElapsed;

  var accuracyColor = summary.accuracy >= 80 ? 'var(--green)' :
                      summary.accuracy >= 50 ? 'var(--yellow)' : 'var(--red)';

  var meta      = EXERCISES.find(function (e) { return e.id === AppState.lastExercise; });
  var drillName = meta ? meta.name.replace('\n', ' ') : '';
  document.querySelector('.summary-header').innerHTML =
    '<p class="summary-drill-name">' + drillName + '</p>' +
    '<h2 class="summary-title">SESSION COMPLETE!</h2>';

  statsEl.innerHTML =
    '<div class="stat-row">' +
      '<span class="stat-label">QUESTIONS</span>' +
      '<span class="stat-value accent">' + summary.total + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">CORRECT</span>' +
      '<span class="stat-value">' + summary.correct + ' / ' + summary.total + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">ACCURACY</span>' +
      '<span class="stat-value" style="color:' + accuracyColor + '">' + summary.accuracy + '%</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">TOTAL TIME</span>' +
      '<span class="stat-value">' + fe(summary.totalMs / 1000, true) + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">AVG TIME</span>' +
      '<span class="stat-value">' + fe(summary.avgSec, true) + '</span>' +
    '</div>' +
    '<div class="stat-row">' +
      '<span class="stat-label">FASTEST</span>' +
      '<span class="stat-value text-yellow">' + fe(summary.fastestSec, true) + '</span>' +
    '</div>';

  document.getElementById('play-again-btn').textContent = 'PLAY AGAIN';

  showScreen('summary');
};
```

---

### FILE 11: `index.html`

**Change (07-06):** Add modal div. Insert before `<script src="app.js"></script>`.

**Find:**
```html
  <script src="app.js"></script>
```
**Replace with:**
```html
  <!-- ===================== RESTART MODAL ===================== -->
  <div id="modal-overlay" class="hidden">
    <div class="modal-box">
      <div class="modal-msg">RESTART DRILL?</div>
      <div class="modal-btns">
        <button class="btn btn-primary" id="modal-confirm-btn">CONFIRM</button>
        <button class="btn btn-secondary" id="modal-cancel-btn">CANCEL</button>
      </div>
    </div>
  </div>

  <script src="app.js"></script>
```

---

### FILE 12: `style.css`

**Change A (07-04):** Reduce `.drill-timer-q` font-size from 10px to 8px.

**Find:**
```css
.drill-timer-q {
  position: absolute;
  top: 12px;
  right: 12px;
  font-size: 10px;
  color: var(--yellow);
}
```
**Replace with:**
```css
.drill-timer-q {
  position: absolute;
  top: 12px;
  right: 12px;
  font-size: 8px;
  color: var(--yellow);
}
```

**Change B (07-02):** Add `.summary-drill-name` before `.summary-title`.

**Find:**
```css
.summary-title {
  font-size: 18px;
  color: var(--yellow);
  text-shadow: 3px 3px 0 #7a6000;
}
```
**Replace with:**
```css
.summary-drill-name {
  font-size: 7px;
  color: var(--blue);
  letter-spacing: 2px;
  margin-bottom: 10px;
}

.summary-title {
  font-size: 18px;
  color: var(--yellow);
  text-shadow: 3px 3px 0 #7a6000;
}
```

**Change C (07-06):** Add modal styles. Insert before the responsive section.

**Find:**
```css
/* ---------- Responsive ---------- */
```
**Replace with:**
```css
/* ---------- Restart modal ---------- */
#modal-overlay {
  position: fixed;
  top: 0; left: 0; width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

.modal-box {
  background: var(--panel);
  border: 2px solid var(--yellow);
  box-shadow: 4px 4px 0 #7a6000, 8px 8px 0 #000;
  padding: 32px 24px;
  text-align: center;
  display: flex;
  flex-direction: column;
  gap: 24px;
  min-width: 220px;
}

.modal-msg {
  font-size: 11px;
  color: var(--yellow);
  letter-spacing: 2px;
  line-height: 2;
}

.modal-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
}

/* ---------- Responsive ---------- */
```

---

## UNIFY — After All Changes Applied

1. Update `.paul/STATE.md`:
   - Mark all Phase 07 plans as DONE
   - Loop Position: all three checkmarks
   - Update "Last activity" and "Next action"

2. Write `.paul/HANDOFF-2026-04-05c.md` (or next date):
   - Summarize what was applied
   - Note any deviations
   - Set next action (Phase 08 or Milestone 5 planning)

3. The old `HANDOFF-2026-04-05b.md` (this file) can be superseded — keep it for history.

---

## Key Files Reference

| File | What changes |
|------|-------------|
| `utils/timer.js` | Already done — formatElapsed added |
| `utils/scoring.js` | getSummary: sumMs, raw floats, totalMs |
| `exercises/ex01` | Remove time-goal, remove icon, timer, feedback time |
| `exercises/ex02` | Enter key, remove icon, timer, feedback time |
| `exercises/ex03` | Remove icon, timer, feedback time |
| `exercises/ex04` | Remove time-goal, timer, fix userVal, remove icon, feedback time |
| `exercises/ex05` | Remove icon, timer, feedback time |
| `exercises/ex06` | Enter key, remove icon, timer, feedback time |
| `exercises/ex07` | Enter key (fill condition), timer |
| `exercises/ex08` | Enter key (fill condition), Enter-to-next, remove icon, timer, feedback time |
| `app.js` | Brackets, ready-hint, restart modal, showSummary |
| `index.html` | Modal div |
| `style.css` | Timer font-size, summary-drill-name, modal styles |

---

*Handoff created: 2026-04-05b — complete apply spec; all find/replace strings exact-match source files*
