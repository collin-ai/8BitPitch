---
phase: 10-av-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [utils/sounds.js, index.html, style.css, exercises/ex01-percentage-snap.js, exercises/ex02-multiples.js, exercises/ex03-double-triple-halve.js, exercises/ex04-triangle-drill.js, exercises/ex05-growth-rate.js, exercises/ex06-break-even.js, exercises/ex07-tam-estimation.js, exercises/ex08-valuation-challenge.js]
autonomous: true
---

<objective>
## Goal
Add audiovisual feedback to all 8 exercises:
- Correct answer → ascending 3-tone chime + short vibration
- Wrong/pass answer → descending buzzer + double vibration
- SFX toggle button in every drill's flex-row (green = on, dim = muted)
- State persists in localStorage

## Output
New `utils/sounds.js` module. SFX button appears in drill flex-row alongside # and PASS. Sounds play on every answer.
</objective>

<tasks>

<task type="auto">
  <name>Create utils/sounds.js</name>
  <files>utils/sounds.js</files>
  <action>
    New Web Audio API module at window.BitPitch.Sound.
    - play(correct): plays correct chime or wrong buzzer + vibration
    - toggle(): flips enabled state, updates all .sound-toggle-btn elements
    - isEnabled(): returns current state
    - State stored in localStorage key 'bp_sound' ('0' = muted)
  </action>
</task>

<task type="auto">
  <name>Add script tag to index.html</name>
  <files>index.html</files>
  <action>
    Add <script src="utils/sounds.js"></script> after numpad.js, before exercises.
  </action>
</task>

<task type="auto">
  <name>Add SFX button CSS to style.css</name>
  <files>style.css</files>
  <action>
    Add .sound-toggle-btn (green border/text = on) and .sound-toggle-btn.muted (dim = off) rules after .numpad-toggle-btn block.
  </action>
</task>

<task type="auto">
  <name>Wire sounds into all 8 exercises</name>
  <files>exercises/ex01–ex08</files>
  <action>
    Each exercise:
    1. Add SFX toggle button to drill card flex-row (after # button)
    2. Call Sound.play() on answer:
       - ex01–ex06, ex04: Sound.play(isOk && !isPassed) at start of showFeedback
       - ex08: Sound.play(overallOk) after overallOk is computed
       - ex07: Sound.play(correct) at start of grade(); Sound.play(false) in pass handler
  </action>
</task>

</tasks>

<verification>
- [ ] SFX button visible in drill flex-row (green when on, dim when muted)
- [ ] Correct answer plays ascending chime
- [ ] Wrong/pass plays descending buzzer
- [ ] SFX button toggles sound on/off; state persists across drills
- [ ] No errors in DevTools console
- [ ] All 8 exercises produce sound
</verification>
