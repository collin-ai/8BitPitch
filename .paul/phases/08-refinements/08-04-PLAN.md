# Plan 08-04: pass-btn Says PASS Only

## What
Change the pass button label from `I'LL PASS` to `PASS` in all exercises.

## Why
The label is long and awkward. `PASS` is cleaner and consistent with how the result is recorded and displayed.

## Files Affected
- `exercises/ex01-percentage-snap.js`
- `exercises/ex02-multiples.js`
- `exercises/ex03-double-triple-halve.js`
- `exercises/ex04-triangle-drill.js`
- `exercises/ex05-growth-rate.js`
- `exercises/ex06-break-even.js`
- `exercises/ex07-tam-estimation.js`
- `exercises/ex08-valuation-challenge.js`

---

## Changes

Apply in every file listed above.

**Find (all occurrences in each file):**
```
'<button class="btn btn-danger" id="pass-btn">I\'LL PASS</button>'
```

**Replace with:**
```
'<button class="btn btn-danger" id="pass-btn">PASS</button>'
```

### ex04 variant (uses double-space before id)
**Find:**
```
'<button class="btn btn-danger"  id="pass-btn">I\'LL PASS</button>'
```

**Replace with:**
```
'<button class="btn btn-danger"  id="pass-btn">PASS</button>'
```

### ex07 variant (different button label for submit)
**Find:**
```
'<button class="btn btn-danger" id="pass-btn">I\'LL PASS</button>'
```

**Replace with:**
```
'<button class="btn btn-danger" id="pass-btn">PASS</button>'
```

---

## Notes
- No JS logic changes. Only the button's visible text changes.
- ex08 uses `id="giveup-btn"` (not `pass-btn`); that button is NOT changed by this plan.
- ex07's submit button label (`CALCULATE MY TAM`) is also unchanged.
