---
phase: 13-teaser-news
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified: [index.html, style.css]
autonomous: false
---

<objective>
## Goal
Populate the 8BitPitch News screen (`#screen-news`) with Coming Soon content items. The screen was built as a shell in Phase 10-03. This plan fills it with real content: a list of upcoming features, a Founder profile blurb, and a Google Form hook section.

## Purpose
The News screen currently shows "COMING SOON" as a placeholder. Giving it real content makes the app feel alive during the beta — it signals active development, sets expectations for future features, and creates a natural place to invite feedback. The Google Form hook converts curious beta users into actionable feedback.

## Output
- `#screen-news` populated with Coming Soon feature cards (static HTML)
- Founder profile blurb section
- Google Form section with link and call-to-action text
- CSS for news screen layout and card styles in style.css
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/ROADMAP.md
@index.html
@style.css
@.paul/phases/10-av-ui-overhaul/10-03-PLAN.md
</context>

<pre-apply-checklist>

## Before Applying This Plan
- [ ] **Phase 10-03 must be complete** — `#screen-news` shell with header and MENU button must exist; this plan fills the body content only
- [ ] **BLOCKING: Resolve Google Form trigger condition** — the original Phase 13 instructions contain "After hitting..." with no completion. Before applying this plan, confirm the full sentence with the user. The Google Form section cannot be written until this is resolved. Raise it explicitly at the start of the PLAN phase.
- [ ] Confirm Coming Soon feature list — suggested items from ROADMAP:
    1. BATTLE PASS — seasonal progression system
    2. DAILY NEWS — real startup/finance news tied to drills
    3. LIVE BATTLES — compete against another player in real time
    4. TEAM BATTLES — squad vs. squad drill competitions
    5. BADGES — achievement system
    6. GAUNTLET MODE — endless drill chain, how far can you go?
  Confirm with user: add, remove, or rename any items before applying
- [ ] Confirm Founder profile content: name, one-line bio, contact — suggested: "BUILT BY COLLIN. ONE FOUNDER. FULL STACK." + email — adjust wording
- [ ] Confirm Google Form CTA text (dependent on resolved trigger condition above)

## Known TBD Items — MUST RESOLVE BEFORE APPLY
- [ ] Google Form trigger condition: "After hitting..." — sentence was cut off in the original instructions. **Ask the user to complete this sentence before writing the Google Form section of this plan.** The rest of the plan (Coming Soon cards, Founder profile) can be drafted but the Google Form section must be confirmed.

</pre-apply-checklist>

<acceptance_criteria>

## AC-1: Coming Soon Feature Cards Visible
Given the user navigates to the 8BITPITCH NEWS tab
When the screen renders
Then at least 4 Coming Soon feature cards are displayed, each with a title and one-line description

## AC-2: Founder Profile Section Visible
Given the news screen is open
When the user scrolls
Then a Founder profile section is visible with a name, one-line bio, and contact email

## AC-3: Google Form Section Visible With CTA
Given the news screen is open and the Google Form trigger condition has been resolved
When the user views the Google Form section
Then a call-to-action matching the confirmed trigger condition is displayed, with a link/button to the Google Form

## AC-4: Google Form Link Opens in New Tab
Given the Google Form section is visible
When the user taps the form button/link
Then https://forms.gle/Q1gpSmKsx6o5AGTT8 opens in a new tab and the news screen remains

## AC-5: MENU Button Returns to Home
Given the news screen is open
When the user taps MENU
Then the home screen is shown (existing behaviour from Phase 10-03 — confirm no regression)

</acceptance_criteria>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Resolve Google Form trigger condition before writing any code</action>
  <steps>
    The original Phase 13 instruction contains "After hitting..." with no completion.
    Ask the user: "What is the full trigger condition for the Google Form CTA on the News screen?
    The original instruction was cut off at 'After hitting...' — please complete the sentence."

    Do not proceed to the HTML task until the full CTA text is confirmed.
    Once confirmed, record it here and use it verbatim in the Google Form section.
  </steps>
  <resume-signal>Type the completed CTA text to unblock this plan</resume-signal>
</task>

<task type="auto">
  <name>Populate #screen-news with Coming Soon cards, Founder profile, and Google Form section</name>
  <files>index.html</files>
  <action>
    In `#screen-news`, after the existing header (with MENU button), replace the "COMING SOON" placeholder with:

    **Coming Soon section:**
    ```html
    <div class="news-section-label">COMING SOON</div>

    <div class="news-card">
      <div class="news-card-title">⚔ BATTLE PASS</div>
      <div class="news-card-desc">SEASONAL PROGRESSION. RANK UP. EARN REWARDS.</div>
    </div>

    <div class="news-card">
      <div class="news-card-title">📰 DAILY NEWS</div>
      <div class="news-card-desc">REAL STARTUP STORIES TURNED INTO DRILLS. EVERY DAY.</div>
    </div>

    <div class="news-card">
      <div class="news-card-title">⚡ LIVE BATTLES</div>
      <div class="news-card-desc">HEAD-TO-HEAD DRILL RACES IN REAL TIME.</div>
    </div>

    <div class="news-card">
      <div class="news-card-title">🛡 TEAM BATTLES</div>
      <div class="news-card-desc">SQUAD VS. SQUAD. WHO HAS THE SHARPEST FOUNDERS?</div>
    </div>

    <div class="news-card">
      <div class="news-card-title">🏅 BADGES</div>
      <div class="news-card-desc">PROVE YOUR SKILLS. COLLECT ACHIEVEMENTS.</div>
    </div>

    <div class="news-card">
      <div class="news-card-title">🔥 GAUNTLET MODE</div>
      <div class="news-card-desc">ENDLESS DRILLS. HOW FAR CAN YOU GO?</div>
    </div>
    ```

    **Founder profile section:**
    ```html
    <div class="news-section-label">THE FOUNDER</div>
    <div class="news-founder-block">
      <div class="news-founder-name">COLLIN</div>
      <div class="news-founder-bio">ONE FOUNDER. FULL STACK. BUILDING IN PUBLIC.</div>
      <div class="news-founder-contact">collin.ai.assistant@gmail.com</div>
    </div>
    ```

    **Google Form section** (content to be filled after trigger condition confirmed — structure below, CTA text is a placeholder until the gate task above is resolved):
    ```html
    <div class="news-section-label">FEEDBACK</div>
    <div class="news-form-block">
      <div class="news-form-cta">[CTA TEXT — CONFIRMED IN PRE-APPLY GATE]</div>
      <a class="btn btn-dim news-form-btn"
         href="https://forms.gle/Q1gpSmKsx6o5AGTT8"
         target="_blank"
         rel="noopener noreferrer">SEND FEEDBACK</a>
    </div>
    ```

    Replace `[CTA TEXT — CONFIRMED IN PRE-APPLY GATE]` with the confirmed trigger condition text before applying.

    Notes:
    - Use Unicode symbols for card icons (no external icon fonts or images)
    - All text uppercase, consistent with app aesthetic
    - Do not add any JS in this task — static HTML only
  </action>
  <verify>Open 8BITPITCH NEWS tab → Coming Soon cards, Founder section, and Feedback section all visible. SEND FEEDBACK → Google Form opens in new tab.</verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Add news screen CSS to style.css</name>
  <files>style.css</files>
  <action>
    Add to style.css:

    ```css
    /* ── News Screen ── */
    #screen-news {
      overflow-y: auto;
    }

    .news-section-label {
      font-family: var(--font);
      font-size: 6px;
      color: var(--dim);
      letter-spacing: 2px;
      margin: 20px 0 8px;
      padding-bottom: 4px;
      border-bottom: 1px solid var(--border);
    }

    .news-card {
      border: 1px solid var(--border);
      padding: 10px 12px;
      margin-bottom: 8px;
    }

    .news-card-title {
      font-family: var(--font);
      font-size: 8px;
      color: var(--fg);
      letter-spacing: 1px;
      margin-bottom: 4px;
    }

    .news-card-desc {
      font-family: var(--font);
      font-size: 6px;
      color: var(--dim);
      letter-spacing: 1px;
      line-height: 1.6;
    }

    .news-founder-block {
      display: flex;
      flex-direction: column;
      gap: 4px;
      padding: 10px 0;
    }

    .news-founder-name {
      font-family: var(--font);
      font-size: 12px;
      color: var(--accent);
      letter-spacing: 3px;
    }

    .news-founder-bio {
      font-family: var(--font);
      font-size: 7px;
      color: var(--fg);
      letter-spacing: 1px;
    }

    .news-founder-contact {
      font-family: var(--font);
      font-size: 6px;
      color: var(--dim);
      letter-spacing: 1px;
    }

    .news-form-block {
      display: flex;
      flex-direction: column;
      gap: 12px;
      padding: 10px 0 20px;
    }

    .news-form-cta {
      font-family: var(--font);
      font-size: 7px;
      color: var(--fg);
      letter-spacing: 1px;
      line-height: 1.8;
    }

    .news-form-btn {
      display: inline-block;
      text-decoration: none;
      text-align: center;
      padding: 8px 16px;
    }
    ```
  </action>
  <verify>News screen scrolls cleanly. Section labels, cards, founder block, and feedback section all render correctly. No overflow or layout breaks.</verify>
  <done>Visual layout complete for all news screen sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>8BitPitch News screen — Coming Soon cards, Founder profile, Google Form feedback section</what-built>
  <how-to-verify>
    1. Open app → tap 8BITPITCH NEWS tab
    2. "COMING SOON" section: 6 feature cards visible, each with title and description
    3. "THE FOUNDER" section: name (COLLIN), bio, and email visible
    4. "FEEDBACK" section: CTA text shown, SEND FEEDBACK button visible
    5. Tap SEND FEEDBACK → https://forms.gle/Q1gpSmKsx6o5AGTT8 opens in new tab
    6. Tap MENU → home screen (no regression)
    7. Screen scrolls smoothly to reach all sections
  </how-to-verify>
  <resume-signal>Type "approved" to continue to UNIFY, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- All exercise files (ex01–ex08.js)
- Scoring, auth, ad system, or any other screen
- Mode bar or tab navigation (established in Phase 10-03)
- News screen header and MENU button (from Phase 10-03)

## SCOPE LIMITS
- This plan is static HTML content only — no dynamic news fetching
- No real-time updates or API calls in this plan
- Coming Soon cards are informational only — no links or interactive elements beyond the Google Form button

</boundaries>

<verification>
- [ ] At least 4 Coming Soon feature cards rendered in news screen
- [ ] Founder profile section visible (name, bio, contact)
- [ ] Google Form CTA text confirmed and matches agreed wording
- [ ] SEND FEEDBACK opens Google Form in new tab
- [ ] MENU button returns to home (no regression)
- [ ] Screen scrolls cleanly on mobile viewport
- [ ] No regressions in any other screen or navigation
</verification>

<success_criteria>
- All 5 acceptance criteria met (AC-3 depends on confirmed CTA text from gate task)
- News screen feels like a product roadmap, not a placeholder
- No regressions in existing app flows
</success_criteria>

<output>
After completion, create `.paul/phases/13-teaser-news/13-03-SUMMARY.md`

Phase 13 is now complete. Milestone 5 (Draft 5) plans are fully written.
Next milestone: Phase 14 (Feedback + Waitlist), then Milestone 6 (Code Cleanup → Final Review → Deploy).
</output>
