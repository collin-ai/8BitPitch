---
phase: 09-refinements
plan: 03
type: implementation
wave: 3
depends_on: ["09-02"]
files_modified:
  - "exercises/ex04-triangle-drill.js"
  - "exercises/ex07-tam-estimation.js"
  - "exercises/ex08-valuation-challenge.js"
  - "exercises/ex01-percentage-snap.js"
  - "exercises/ex02-multiples.js"
  - "exercises/ex03-double-triple-halve.js"
  - "exercises/ex05-growth-rate.js"
  - "exercises/ex06-break-even.js"
  - "app.js"
  - "style.css"
  - "index.html"
  - "utils/random.js"
  - "tests/tests.html"
autonomous: true
---

<objective>
## Goal
Apply all code review fixes identified in Phase 09-02 that are scoped for Milestone 4. Fixes are grouped into three work streams: medium-severity correctness bugs, selected low-severity items, and a new testing harness.

## Purpose
Close out Milestone 4. The three medium findings are correctness bugs that affect game fairness and session accuracy. The low-severity items are quick wins with no structural risk. The testing harness validates the pure utility functions that underpin all grading decisions.

## Output
- All 3 medium bugs fixed (C-1, C-2, C-3)
- 8 low-severity items resolved (C-4, R-2, W-1, W-2, S-1, S-2, E-1, E-3)
- `tests/tests.html` created and passing all assertions
</objective>

<context>
@.paul/phases/09-refinements/09-02-REVIEW.md
@exercises/ex01-percentage-snap.js
@exercises/ex04-triangle-drill.js
@exercises/ex07-tam-estimation.js
@exercises/ex08-valuation-challenge.js
@exercises/ex02-multiples.js
@exercises/ex03-double-triple-halve.js
@exercises/ex05-growth-rate.js
@exercises/ex06-break-even.js
@app.js
@style.css
@index.html
@utils/random.js
@utils/scoring.js
@utils/timer.js
</context>

<acceptance_criteria>
## AC-1: C-1 Fixed — ex04 difficulty respected
Given ex04 is loaded at EASY, MEDIUM, or HARD difficulty
When the player submits an answer
Then the tolerance applied matches DIFFICULTY_CONFIG for that level (same logic as ex01 lines 97–106)

## AC-2: C-2 Fixed — ex08 Q3 counts toward score
Given ex08 is running a round
When the player submits all three answers
Then allOk = q1Ok && q2Ok && q3Ok — a wrong Q3 produces an incorrect result

## AC-3: C-3 Fixed — ex07 thumb buttons disabled after first click
Given ex07 is showing self-grade thumbs
When the player clicks either thumb
Then both buttons are disabled before onComplete fires — a second click has no effect

## AC-4: Low-severity items resolved
Given the codebase after this plan is applied
When reviewed against the 09-02 findings
Then C-4, R-2, W-1, W-2, S-1, S-2, E-1, E-3 are all addressed

## AC-5: tests/tests.html exists and passes
Given tests/tests.html is opened in a browser
When all assertions run
Then every test shows PASS with zero FAIL entries on the page
</acceptance_criteria>

<tasks>

<!-- ═══════════════════════════════════════════════════════════
     GROUP A — MEDIUM SEVERITY
     ═══════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>C-1: Fix ex04 difficulty grading</name>
  <files>exercises/ex04-triangle-drill.js, exercises/ex01-percentage-snap.js</files>
  <action>
    Read ex01 lines 97–106 to see the exact DIFFICULTY_CONFIG lookup pattern.
    In ex04 `submit()`, replace the single hardcoded line:
      `var isOk = close(userVal, correctAnswer, 5);`
    with the same conditional block from ex01:
      - If type === 'ask' && tri.equity === 33: check difficulty
        - HARD → isCloseEnough2dp(rawVal, correctAnswer)
        - else  → close(rawVal, correctAnswer, cfg.pct33) where cfg = DIFFICULTY_CONFIG[context.difficulty] || { pct33: 5 }
      - All other cases → close(userVal, correctAnswer, 5)
    Verify the variable names match ex04's local scope (rawInput → userVal, input.value already parsed).
  </action>
  <verify>
    Manually: launch ex04 at EASY, answer ~±25% off for a ÷3 question — should pass.
    Launch at HARD with the same answer — should fail.
    Launch at MEDIUM — should apply medium tolerance.
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>C-2: Fix ex08 Q3 not counted in score</name>
  <files>exercises/ex08-valuation-challenge.js</files>
  <action>
    Find line 146: `var allOk = q1Ok && q2Ok;`
    Change to: `var allOk = q1Ok && q2Ok && q3Ok;`
    No other changes — q3Ok is already computed on line 144.
  </action>
  <verify>
    Run ex08. Answer Q1 and Q2 correctly but Q3 wildly wrong.
    Result should be WRONG, not "SHARP INVESTOR!".
  </verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>C-3: Disable ex07 thumb buttons on first click</name>
  <files>exercises/ex07-tam-estimation.js</files>
  <action>
    Find the `grade(correct)` function (around line 161).
    Add these two lines as the very first statements inside `grade()`, before anything else:
      document.getElementById('thumb-up').disabled   = true;
      document.getElementById('thumb-down').disabled = true;
    This ensures a double-click or simultaneous click cannot fire onComplete twice.
  </action>
  <verify>
    Run ex07. Click a thumb button rapidly twice.
    Session summary should show the same number of answers as questions — no duplicates.
  </verify>
  <done>AC-3 satisfied</done>
</task>

<!-- ═══════════════════════════════════════════════════════════
     GROUP B — LOW SEVERITY
     ═══════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>C-4: Fix replace('\n') to use regex in app.js</name>
  <files>app.js</files>
  <action>
    Find both occurrences of `replace('\n', '<br>')` (around lines 59 and 103).
    Change each to `replace(/\n/g, '<br>')`.
    There are exactly two occurrences — confirm both are updated.
  </action>
  <verify>No exercise name currently has two newlines, so this is a latent fix — confirm both lines changed.</verify>
  <done>C-4 resolved</done>
</task>

<task type="auto">
  <name>R-2: Remove duplicate Google Fonts @import from style.css</name>
  <files>style.css</files>
  <action>
    Find the `@import` line at the top of style.css that loads the Google Fonts URL.
    Delete that line entirely. The `<link>` tag in index.html already loads the same font and takes precedence.
    Do not touch index.html.
  </action>
  <verify>Open app — font renders correctly. No visual change expected.</verify>
  <done>R-2 resolved</done>
</task>

<task type="auto">
  <name>W-1: Add favicon suppression to index.html</name>
  <files>index.html</files>
  <action>
    In `<head>`, after the existing `<link>` tags, add:
      `<link rel="icon" href="data:,">`
    This suppresses the browser's automatic /favicon.ico request with a zero-byte data URI.
  </action>
  <verify>Open browser dev tools Network tab — no favicon 404.</verify>
  <done>W-1 resolved</done>
</task>

<task type="auto">
  <name>W-2: Add meta description and OG tags to index.html</name>
  <files>index.html</files>
  <action>
    In `<head>`, add after the favicon line:
      `<meta name="description" content="8BitPitch Lite — sharpen your startup finance mental math with 8 retro-style drills.">`
      `<meta property="og:title" content="8BitPitch Lite">`
      `<meta property="og:description" content="8 mental math drills for investors and entrepreneurs. Valuation, break-even, TAM, growth rates — in an 8-bit retro game.">`
      `<meta property="og:type" content="website">`
  </action>
  <verify>View page source — four new meta lines present in head.</verify>
  <done>W-2 resolved</done>
</task>

<task type="auto">
  <name>S-1: Add esc() helper and sanitize rawInput in all showFeedback functions</name>
  <files>app.js, exercises/ex01-percentage-snap.js, exercises/ex02-multiples.js, exercises/ex03-double-triple-halve.js, exercises/ex04-triangle-drill.js, exercises/ex05-growth-rate.js, exercises/ex06-break-even.js, exercises/ex07-tam-estimation.js, exercises/ex08-valuation-challenge.js</files>
  <action>
    Step 1 — Add helper to app.js, near the top of the file (after the strict mode declaration or the first comment block):
      function esc(s) {
        var d = document.createElement('div');
        d.textContent = String(s);
        return d.innerHTML;
      }

    Step 2 — In each exercise's showFeedback function, find every place where the raw user input string (rawInput, rawVal, rawMult, rawBE, or equivalent) is concatenated directly into an innerHTML string.
    Wrap each occurrence with esc(): e.g. `esc(rawInput)`.

    Notes:
    - Only wrap values that come from user input (input.value). Do NOT wrap computed values like formatMoney(correctAnswer) — those are internal.
    - ex08 has three input fields (rawVal, rawMult, rawBE) — wrap all three.
    - ex07 is self-graded; there is no raw text input to wrap — skip.
    - If rawInput is null (pass flow), esc(null) will produce "null" — ensure pass flow passes a string like 'PASS' or check before calling esc().
  </action>
  <verify>
    Open browser console. Paste `<img src=x onerror=alert(1)>` into any drill input and submit.
    No alert fires. The string appears as literal text in the feedback box.
  </verify>
  <done>AC-4 (S-1) satisfied</done>
</task>

<task type="auto">
  <name>S-2: Add inline-onclick warning comment to app.js</name>
  <files>app.js</files>
  <action>
    Find the `var AppState` declaration (or `window.AppState` / `AppState = {...}` — wherever AppState is first defined).
    Add a single-line comment directly above it:
      // NOTE: 'AppState' is referenced by name in inline onclick attributes in index.html — do not rename without updating HTML.
    Find where `window.BitPitch` or `BitPitch` namespace is first established and add a similar comment if it is not already obvious.
  </action>
  <verify>Comment present; no functional change.</verify>
  <done>S-2 resolved</done>
</task>

<task type="auto">
  <name>E-1: Clear document.onkeydown on navigation in app.js</name>
  <files>app.js</files>
  <action>
    Find `AppState.goHome()` (or equivalent home navigation function). Add `document.onkeydown = null;` as the first statement inside it.
    Find `AppState.startExercise()` (or where a new exercise is launched). Add `document.onkeydown = null;` as the first statement inside it, before any exercise-specific setup.
    This ensures any stale Enter-to-NEXT handler from a previous feedback screen is cleared before the new screen renders.
  </action>
  <verify>
    Run a drill to feedback screen. Press MENU. On the home screen, press Enter. Nothing unexpected fires.
    Start a new drill immediately after — Enter key works correctly for the new drill.
  </verify>
  <done>E-1 resolved</done>
</task>

<task type="auto">
  <name>E-3: Guard parseUserNumber against non-finite and negative values</name>
  <files>utils/random.js</files>
  <action>
    Find `parseUserNumber` (around line 86). After the `parseFloat` call that produces the numeric value `n`, add:
      if (!isFinite(n) || n < 0) return NaN;
    Place this check before the value is returned, after all suffix substitutions have been applied.
    This rejects: scientific notation that produces Infinity, negative inputs, NaN that slipped through.
  </action>
  <verify>
    In browser console: BitPitch.parseUserNumber('1e308') → NaN. BitPitch.parseUserNumber('-5') → NaN.
    BitPitch.parseUserNumber('$1.5M') → 1500000 (still works).
  </verify>
  <done>E-3 resolved</done>
</task>

<!-- ═══════════════════════════════════════════════════════════
     GROUP C — TESTING HARNESS
     ═══════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>T-1/T-2/T-3: Create tests/tests.html</name>
  <files>tests/tests.html</files>
  <action>
    Create a new file `tests/tests.html`. It must:
    1. Load the same utility scripts as index.html (relative paths): utils/random.js, utils/scoring.js, utils/timer.js
    2. Define a minimal test harness inline (no framework):
       - `assert(label, actual, expected)` — compares with === (or isNaN check for NaN cases); logs PASS/FAIL to the page
       - Display a summary count at the bottom: "X passed, Y failed"
    3. Run the following test groups:

    **T-1a — parseUserNumber**
    - "$1.5M"  → 1500000
    - "500K"   → 500000
    - "1,000"  → 1000
    - ""       → NaN
    - "abc"    → NaN
    - "0"      → 0
    - "1.2B"   → 1200000000
    - "-5"     → NaN   (after E-3 fix)
    - "1e6"    → NaN   (after E-3 fix — scientific notation rejected)

    **T-1b — formatMoney**
    - 1000000  → "$1M" (or whatever the app's format is — check the function)
    - 500000   → "$500K"
    - 1500     → "$1,500" (or formatted equivalent)

    **T-1c — isCloseEnough**
    - isCloseEnough(105, 100, 5)  → true  (exactly at boundary)
    - isCloseEnough(106, 100, 5)  → false (one unit beyond — note: depends on implementation; adjust if boundary is exclusive)
    - isCloseEnough(95, 100, 5)   → true
    - isCloseEnough(94, 100, 5)   → false
    - isCloseEnough(0, 0, 5)      → true  (zero case)

    **T-2 — isCloseEnough boundary (inclusive)**
    - isCloseEnough(105, 100, 5)  → true   (exactly 5% off — must pass per T-2 finding)
    - isCloseEnough(105.01, 100, 5) → false (beyond boundary — may need float tolerance)

    **T-1d — isCloseEnough2dp**
    - isCloseEnough2dp(1.33, 1.33) → true
    - isCloseEnough2dp(1.34, 1.33) → false
    - isCloseEnough2dp(33.33, 33.33) → true

    **T-3a — ex06 scenario sanity**
    Copy the scenario array from ex06 into a local variable in the test script.
    For each scenario: assert revenue - costs === profit (within floating point tolerance).
    Assert overhead / profit rounded to ceiling === breakEvenMonths (or however ex06 computes it).

    **T-3b — TAM category sanity**
    Copy the TAM categories array from utils/random.js (or ex07 if it's local there).
    For each category: assert customers * spend === answer.

    Style: display results as a simple list. Green text for PASS, red for FAIL. Summary line at bottom.
    The file must work when opened at file:// (no fetch, no require).
  </action>
  <verify>
    Open tests/tests.html in browser. All test rows show PASS. Summary shows "X passed, 0 failed".
    If any assertion fails, it means a utility function is broken or the test expectation is wrong — investigate before marking done.
  </verify>
  <done>AC-5 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Exercise scoring logic beyond what is listed in Group A
- Timer behaviour (intervals, thresholds)
- Any numpad.js code
- style.css beyond the @import removal
- The PAUL system files (other than what this plan produces)

## SCOPE LIMITS
- Deferred items (C-5, R-1, R-3, R-4, R-5, E-2, W-4, T-4) — do NOT fix in this plan; they are Milestone 6 work
- Do not restructure or refactor any exercise beyond the specific lines called out per task
- tests/tests.html is a read-only diagnostic tool — it must not modify any app state or files
</boundaries>

<verification>
After all tasks are complete, verify the following:
1. Run all 8 drills in sequence — no console errors
2. ex04 at HARD difficulty: a ÷3 answer must be exact to 2dp to pass
3. ex08: a wrong Q3 produces an incorrect result
4. ex07: double-clicking a thumb button does not add extra rows to session summary
5. Open tests/tests.html — 0 failures
6. View page source — favicon link and meta tags present in head
7. Paste XSS payload into any drill input — no alert fires
</verification>

<success_criteria>
- [ ] C-1 fixed: ex04 difficulty applies correct tolerance per DIFFICULTY_CONFIG
- [ ] C-2 fixed: ex08 allOk includes q3Ok
- [ ] C-3 fixed: ex07 thumb buttons disabled before onComplete
- [ ] C-4 fixed: app.js uses /\n/g regex
- [ ] R-2 fixed: @import removed from style.css
- [ ] W-1 fixed: favicon link present in index.html head
- [ ] W-2 fixed: meta description and OG tags in index.html head
- [ ] S-1 fixed: esc() helper in app.js; rawInput wrapped in all showFeedback functions
- [ ] S-2 fixed: AppState rename comment in app.js
- [ ] E-1 fixed: document.onkeydown = null in goHome() and startExercise()
- [ ] E-3 fixed: parseUserNumber rejects non-finite and negative values
- [ ] tests/tests.html created; all assertions pass
- [ ] No regressions in any of the 8 exercises
</success_criteria>
